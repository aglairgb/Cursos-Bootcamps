Abordado no curso como:

- mudar de pastas;
- listar as pastas;
- criar pastas/arquivos;
- deletar pastas/arquivos.

windows/linux

windows(shell) :

- cd.. (retrocede um diretório)
- cd (abre diretório) 
- dir (mostra diretórios na pasta situada)
- mkdir
- del / rmdir
- cls (limpa terminal)
- TAB - autocompleta
- echo ( exemplo: hello) // printa o que é escrito
- echo > teste.txt // cria documento, arquivo
- del (apaga conteúdo de um diretório)
- rmdir (apaga diretório)


linux (bash) :

-  cd (abre diretório)
-  ls (mostra diretórios na pasta situada)
-  mkdir
- -rm -rf 
- cd.. (retrocede um diretório)
- clear (limpa terminal) ou (ctrl + L)
- rm -rf (limpa diretório) 
