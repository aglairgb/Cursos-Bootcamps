 const pontuacaoUsuario = 1000;
 const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usuário VIP' : 'Usuário padrão';

 console.log(nivelUsuario);

// return (pontuacaoUsuario >= 1000) ? console.log('Usuário vip') : console.log('Usuário padrão');
